const Sensor = require("../model/Sensor.model.js");

class SensorController {
  // static async updateSensorStatus(request, response) {
  //     try {
  //         const {sensorId} = request.params;
  //         const {status}
  //     } catch (error) {
  //         response.status(500).json({success: false, message: "Failed to update sensor data", error: error.message});
  //     }
  // }
}

module.exports = SensorController;
